# Redis config rendered by scripts/render-config.sh from redis.conf.template.
# Do not put secrets in Git.

bind 0.0.0.0
protected-mode yes
port 6379
dir /data
appendonly yes
appendfsync everysec
maxmemory ${REDIS_MAXMEMORY}
maxmemory-policy allkeys-lru
tcp-keepalive 300
timeout 0
